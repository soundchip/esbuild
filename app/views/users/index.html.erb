<style>
.myDiv {  
  margin: auto;
  width: 60%;
  border: 0px;
  padding: 10px;
}
</style>

<div class="myDiv">
  <p style="color: green"><%= notice %></p>
  <h1>Users</h1>

  <p><%= button_to 'NEW USER', new_user_path, method: :get%></p>

  <table cellspacing ="1" width="50%" align="left" >
    <thead>
      <tr align="left">
        <th width="50px">ID</th>
        <th width="300x">Name</th>
        <th width="150px">Role</th>
        <th width="150px"></th>
      </tr>
    </thead>
      <tbody align="left">    
      <% @users.each do |user| %>    
        <tr>
          <td width="50px"><%= user.id%></td>
          <td width="300px"><%= link_to user.name, user%></td>        
          <td width="150px"><%= user.role %></td>                   
          <td><%= link_to "Delete", user_path(user), data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure?"
                    } %>
        </tr>     
      <% end %>
    </tbody>
  </table>
</div>


